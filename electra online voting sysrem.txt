<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ELECTRA - Secure Voting Portal</title>
    
    <!-- Load Tailwind CSS -->
    <script src="https://cdn.tailwindcss.com"></script>
    
    <!-- Load React and ReactDOM -->
    <script crossorigin src="https://unpkg.com/react@18/umd/react.development.js"></script>
    <script crossorigin src="https://unpkg.com/react-dom@18/umd/react-dom.development.js"></script>
    
    <!-- Load Lucide Icons -->
    <script src="https://unpkg.com/lucide@latest"></script>
    <script src="https://unpkg.com/lucide-react/dist/lucide-react.min.js"></script>
    
    <!-- Load Babel for JSX -->
    <script src="https://unpkg.com/@babel/standalone/babel.min.js"></script>

    <style>
        body { margin: 0; background-color: #020617; font-family: sans-serif; overflow-x: hidden; }
        
        /* --- ANIMATIONS --- */
        
        /* Smooth Fade In & Slide Up */
        @keyframes slideUpFade { 
            from { opacity: 0; transform: translateY(20px); } 
            to { opacity: 1; transform: translateY(0); } 
        }
        .animate-slide-up { animation: slideUpFade 0.6s cubic-bezier(0.16, 1, 0.3, 1) forwards; opacity: 0; }
        
        /* Floating Animation for Icons */
        @keyframes float {
            0%, 100% { transform: translateY(0px); }
            50% { transform: translateY(-10px); }
        }
        .animate-float { animation: float 4s ease-in-out infinite; }
        
        /* Slow Rotation for Background Nebula */
        @keyframes slowSpin {
            from { transform: rotate(0deg); }
            to { transform: rotate(360deg); }
        }
        .animate-spin-slow { animation: slowSpin 120s linear infinite; }

        /* Neon Pulse */
        @keyframes neonPulse {
            0%, 100% { box-shadow: 0 0 5px rgba(6, 182, 212, 0.5), 0 0 10px rgba(6, 182, 212, 0.3); }
            50% { box-shadow: 0 0 20px rgba(6, 182, 212, 0.8), 0 0 25px rgba(6, 182, 212, 0.5); }
        }
        .animate-neon { animation: neonPulse 2s infinite; }

        /* Staggered Delays */
        .delay-100 { animation-delay: 100ms; }
        .delay-200 { animation-delay: 200ms; }
        .delay-300 { animation-delay: 300ms; }
    </style>
</head>
<body>
    <div id="root"></div>

    <script type="text/babel">
        const { useState, useEffect, useRef } = React;
        const { User, Lock, FileText, Settings, Fingerprint, LogOut, CheckCircle, Camera, Mic, Bell, ChevronLeft, Shield, AlertTriangle, RefreshCw } = lucide;

        // --- MOCK DATA ---
        const MOCK_USER = {
          name: "Anany Bhargava",
          age: 18, 
          aadhar: "4589 1256 7890",
          pan: "ABCDE1234F",
          constituency: "Bangalore South",
          voted: false
        };

        const CANDIDATES = [
          {
            id: 1,
            name: "Rajesh Kumar",
            party: "BJP",
            symbol: "🪷",
            agenda: "Nation First & Development",
            color: "from-orange-400 to-orange-600"
          },
          {
            id: 2,
            name: "Priya Sharma",
            party: "Congress",
            symbol: "✋",
            agenda: "Justice & Employment",
            color: "from-blue-400 to-blue-600"
          },
          {
            id: 3,
            name: "Amit Patel",
            party: "Aam Aadmi Party",
            symbol: "🧹",
            agenda: "Corruption Free Governance",
            color: "from-yellow-400 to-yellow-600"
          }
        ];

        const ANNOUNCEMENTS = [
          {
            id: 1,
            candidateId: 2,
            title: "Town Hall Tomorrow!",
            content: "Join me at the City Center for a discussion on our new digital literacy program.",
            time: "2 hours ago"
          },
          {
            id: 2,
            candidateId: 1,
            title: "New Park Initiative",
            content: "We are pledging to plant 10,000 trees in the district if elected.",
            time: "5 hours ago"
          }
        ];

        // --- COMPONENTS ---

        const StarryBackground = () => (
          <div className="fixed inset-0 z-0 overflow-hidden pointer-events-none bg-slate-950">
            {/* Rotating Nebula Background */}
            <div className="absolute -top-[50%] -left-[50%] w-[200%] h-[200%] bg-[radial-gradient(ellipse_at_center,_rgba(15,23,42,0.5)_0%,_rgba(2,6,23,1)_70%)] animate-spin-slow opacity-60"></div>
            
            {/* Deep Space Gradient */}
            <div className="absolute inset-0 bg-gradient-to-b from-transparent via-slate-950/80 to-slate-950"></div>

            {/* Dynamic Stars */}
            {Array.from({ length: 60 }).map((_, i) => (
              <div
                key={i}
                className="absolute rounded-full bg-white opacity-0 animate-pulse"
                style={{
                  top: Math.random() * 100 + '%',
                  left: Math.random() * 100 + '%',
                  width: (Math.random() * 2 + 1) + 'px',
                  height: (Math.random() * 2 + 1) + 'px',
                  animationDuration: (Math.random() * 3 + 2) + 's',
                  animationDelay: (Math.random() * 5) + 's',
                  boxShadow: `0 0 ${Math.random() * 4 + 1}px rgba(255, 255, 255, 0.8)`
                }}
              />
            ))}
            
            {/* Shooting star */}
            <div className="absolute top-0 left-0 w-full h-full overflow-hidden">
                <div className="absolute top-[10%] left-[20%] w-[2px] h-[100px] bg-gradient-to-b from-transparent via-cyan-400 to-transparent rotate-45 opacity-0 animate-[shooting_7s_ease-in-out_infinite]"></div>
                <div className="absolute top-[30%] left-[60%] w-[1px] h-[80px] bg-gradient-to-b from-transparent via-purple-400 to-transparent rotate-[30deg] opacity-0 animate-[shooting_10s_ease-in-out_2s_infinite]"></div>
            </div>
            <style>{`
              @keyframes shooting {
                0% { transform: translate(-100px, -100px) rotate(45deg); opacity: 1; }
                20% { transform: translate(300px, 300px) rotate(45deg); opacity: 0; }
                100% { transform: translate(300px, 300px) rotate(45deg); opacity: 0; }
              }
            `}</style>
          </div>
        );

        function App() {
          const [page, setPage] = useState('login'); 
          const [isAuthenticated, setIsAuthenticated] = useState(false);
          const [currentUser, setCurrentUser] = useState(null);
          const [loginData, setLoginData] = useState({ aadhar: '', pan: '' });
          const [voteStep, setVoteStep] = useState('list');
          const [selectedCandidate, setSelectedCandidate] = useState(null);
          const [cameraActive, setCameraActive] = useState(false);
          const videoRef = useRef(null);
          const streamRef = useRef(null);

          useEffect(() => {
            lucide.createIcons();
          }); 

          const handleAadharChange = (e) => {
            let val = e.target.value.replace(/[^0-9]/g, '');
            if (val.length > 12) val = val.slice(0, 12);
            val = val.replace(/(\d{4})(?=\d)/g, '$1 ');
            setLoginData({ ...loginData, aadhar: val });
          };

          const handleLogin = (e) => {
            e.preventDefault();
            if (loginData.aadhar && loginData.pan) {
              setIsAuthenticated(true);
              setCurrentUser(MOCK_USER);
              setPage('home');
            } else {
              alert("Please enter valid credentials");
            }
          };

          const handleLogout = () => {
            setIsAuthenticated(false);
            setCurrentUser(null);
            setPage('login');
            setVoteStep('list');
            setLoginData({ aadhar: '', pan: '' });
          };

          const startVoting = (candidate) => {
            if (currentUser.voted) return;
            setSelectedCandidate(candidate);
            setVoteStep('face');
            setCameraActive(true);
          };

          const resetVote = () => {
              setCurrentUser(prev => ({...prev, voted: false}));
              setSelectedCandidate(null);
              setVoteStep('list');
          };

          useEffect(() => {
            let timeout = null;
            let mounted = true;

            if (voteStep === 'face' && cameraActive) {
              const startCamera = async () => {
                try {
                  const stream = await navigator.mediaDevices.getUserMedia({ video: true });
                  if (!mounted) {
                    stream.getTracks().forEach(track => track.stop());
                    return;
                  }
                  streamRef.current = stream;
                  if (videoRef.current) {
                    videoRef.current.srcObject = stream;
                  }
                  
                  timeout = setTimeout(() => {
                    if (mounted) {
                      setCameraActive(false);
                      setVoteStep('fingerprint');
                    }
                  }, 3500);
                } catch (err) {
                  console.log("Camera simulation fallback");
                  timeout = setTimeout(() => {
                    if (mounted) {
                       setCameraActive(false);
                       setVoteStep('fingerprint');
                    }
                  }, 2000);
                }
              };
              startCamera();
            }

            return () => {
              mounted = false;
              if (streamRef.current) {
                streamRef.current.getTracks().forEach(track => track.stop());
                streamRef.current = null;
              }
              if (timeout) clearTimeout(timeout);
            };
          }, [voteStep, cameraActive]);

          const handleFingerprintScan = () => {
            const btn = document.getElementById('fingerprint-btn');
            if (btn) btn.classList.add('animate-neon', 'text-cyan-400', 'scale-110');
            
            setTimeout(() => {
              setVoteStep('success');
              setCurrentUser(prev => ({...prev, voted: true}));
            }, 2000);
          };

          // --- Views ---

          const renderLogin = () => (
            <div className="relative z-10 flex flex-col items-center justify-center min-h-screen p-4 text-white">
              <div className="w-full max-w-md p-8 border border-slate-700/50 shadow-2xl bg-slate-900/60 backdrop-blur-xl rounded-3xl animate-slide-up hover:border-cyan-500/30 transition-colors duration-500">
                <div className="flex flex-col items-center mb-8">
                  <div className="flex items-center justify-center w-20 h-20 mb-4 rounded-full bg-gradient-to-br from-cyan-500 to-blue-600 shadow-[0_0_30px_rgba(6,182,212,0.6)] animate-float">
                    <i data-lucide="check-circle" className="w-10 h-10 text-white"></i>
                  </div>
                  <h1 className="text-5xl font-bold tracking-wider text-transparent bg-clip-text bg-gradient-to-r from-cyan-400 to-blue-500 drop-shadow-sm">ELECTRA</h1>
                  <p className="mt-2 text-sm text-slate-400 tracking-widest uppercase">Secure National Voting Portal</p>
                </div>

                <form onSubmit={handleLogin} className="space-y-6">
                  <div className="space-y-2 group">
                    <label className="text-xs font-semibold tracking-wide uppercase text-slate-400 group-focus-within:text-cyan-400 transition-colors">Aadhar Number</label>
                    <div className="relative">
                      <i data-lucide="fingerprint" className="absolute left-3 top-3 text-slate-500 w-5 h-5 group-focus-within:text-cyan-400 transition-colors"></i>
                      <input 
                        type="text" 
                        inputMode="numeric"
                        maxLength="14"
                        placeholder="0000 0000 0000"
                        className="w-full pl-10 pr-4 py-3 transition-all duration-300 border rounded-xl outline-none bg-slate-800/50 border-slate-700 text-slate-100 focus:border-cyan-500 focus:ring-1 focus:ring-cyan-500 focus:bg-slate-800 placeholder:text-slate-600 hover:bg-slate-800/80"
                        value={loginData.aadhar}
                        onChange={handleAadharChange}
                      />
                    </div>
                  </div>
                  <div className="space-y-2 group">
                    <label className="text-xs font-semibold tracking-wide uppercase text-slate-400 group-focus-within:text-cyan-400 transition-colors">PAN Card ID</label>
                    <div className="relative">
                      <i data-lucide="shield" className="absolute left-3 top-3 text-slate-500 w-5 h-5 group-focus-within:text-cyan-400 transition-colors"></i>
                      <input 
                        type="text" 
                        placeholder="ABCDE1234F"
                        className="w-full pl-10 pr-4 py-3 transition-all duration-300 border rounded-xl outline-none bg-slate-800/50 border-slate-700 text-slate-100 focus:border-cyan-500 focus:ring-1 focus:ring-cyan-500 focus:bg-slate-800 placeholder:text-slate-600 hover:bg-slate-800/80"
                        value={loginData.pan}
                        onChange={(e) => setLoginData({...loginData, pan: e.target.value})}
                      />
                    </div>
                  </div>
                  <button type="submit" className="w-full py-4 mt-4 font-bold tracking-wide text-white transition-all duration-300 rounded-xl shadow-lg bg-gradient-to-r from-cyan-600 to-blue-700 hover:from-cyan-500 hover:to-blue-600 hover:scale-[1.02] hover:shadow-cyan-500/25 active:scale-95 animate-slide-up delay-100">
                    AUTHENTICATE & ENTER
                  </button>
                </form>
              </div>
            </div>
          );

          const renderHome = () => (
            <div className="relative z-10 w-full max-w-4xl p-6 mx-auto mt-10">
              <header className="flex items-center justify-between mb-10 animate-slide-up">
                <div>
                   <h2 className="text-3xl font-bold text-white drop-shadow-md">Welcome, <span className="text-transparent bg-clip-text bg-gradient-to-r from-cyan-400 to-blue-400">{currentUser.name.split(' ')[0]}</span></h2>
                   <p className="text-slate-400">Your voice matters. Vote responsibly.</p>
                </div>
                <div onClick={() => setPage('profile')} className="p-1 transition-all duration-300 border-2 rounded-full cursor-pointer border-cyan-500/50 hover:border-cyan-400 hover:shadow-[0_0_15px_rgba(6,182,212,0.4)] bg-slate-800 flex items-center justify-center w-14 h-14 hover:scale-110 active:scale-95 group">
                   <i data-lucide="user" className="w-7 h-7 text-cyan-400 group-hover:text-white transition-colors"></i>
                </div>
              </header>

              <div className="grid grid-cols-1 gap-6 md:grid-cols-2">
                <div 
                    onClick={() => setPage('vote')}
                    className="relative p-8 overflow-hidden transition-all duration-500 cursor-pointer group bg-gradient-to-br from-indigo-900/40 to-slate-900/60 backdrop-blur-md rounded-3xl border border-indigo-500/30 hover:border-indigo-400 hover:shadow-[0_0_40px_rgba(99,102,241,0.25)] hover:-translate-y-1 md:col-span-2 animate-slide-up delay-100"
                >
                    <div className="relative z-10 flex flex-col items-start justify-between h-full md:flex-row md:items-center">
                        <div>
                            <h3 className="mb-2 text-3xl font-bold text-white group-hover:text-indigo-300 transition-colors">Cast Your Vote</h3>
                            <p className="text-indigo-200">Constituency: <span className="font-semibold text-white">{currentUser.constituency}</span></p>
                            <div className="mt-6 inline-flex items-center px-6 py-2.5 text-sm font-bold text-white rounded-full bg-indigo-600 group-hover:bg-indigo-500 shadow-lg shadow-indigo-900/50 transition-all">
                                {currentUser.voted ? "View Status" : "Proceed to Polls"} <i data-lucide="chevron-left" className="w-4 h-4 ml-2 rotate-180 group-hover:translate-x-1 transition-transform"></i>
                            </div>
                        </div>
                        <div className="mt-4 md:mt-0">
                            <i data-lucide="fingerprint" className="w-28 h-28 text-indigo-500/50 group-hover:text-indigo-400 group-hover:scale-110 group-hover:rotate-12 transition-all duration-500"></i>
                        </div>
                    </div>
                    {/* Decorative glow */}
                    <div className="absolute top-0 right-0 w-64 h-64 bg-indigo-600 rounded-full opacity-10 blur-[80px] -translate-y-1/2 translate-x-1/2 group-hover:opacity-20 transition-opacity duration-500"></div>
                </div>

                <div 
                    onClick={() => setPage('announcements')}
                    className="p-6 transition-all duration-300 cursor-pointer bg-slate-900/40 backdrop-blur border border-slate-700 hover:border-cyan-500 rounded-3xl hover:bg-slate-800/60 hover:shadow-[0_0_20px_rgba(6,182,212,0.15)] hover:-translate-y-1 group animate-slide-up delay-200"
                >
                    <div className="w-12 h-12 rounded-xl bg-cyan-900/20 flex items-center justify-center mb-4 group-hover:bg-cyan-900/40 transition-colors">
                        <i data-lucide="bell" className="w-6 h-6 text-cyan-400 group-hover:scale-110 transition-transform"></i>
                    </div>
                    <h3 className="text-xl font-bold text-white">Announcements</h3>
                    <p className="text-sm text-slate-400 mt-1">View latest updates and campaign agendas.</p>
                </div>

                <div 
                    onClick={() => setPage('settings')}
                    className="p-6 transition-all duration-300 cursor-pointer bg-slate-900/40 backdrop-blur border border-slate-700 hover:border-cyan-500 rounded-3xl hover:bg-slate-800/60 hover:shadow-[0_0_20px_rgba(6,182,212,0.15)] hover:-translate-y-1 group animate-slide-up delay-300"
                >
                    <div className="w-12 h-12 rounded-xl bg-cyan-900/20 flex items-center justify-center mb-4 group-hover:bg-cyan-900/40 transition-colors">
                        <i data-lucide="settings" className="w-6 h-6 text-cyan-400 group-hover:rotate-45 transition-transform duration-500"></i>
                    </div>
                    <h3 className="text-xl font-bold text-white">Settings</h3>
                    <p className="text-sm text-slate-400 mt-1">Manage notifications and privacy.</p>
                </div>
              </div>
            </div>
          );

          const renderVote = () => {
            if (currentUser.voted && voteStep !== 'success') {
                return (
                    <div className="relative z-10 flex flex-col items-center justify-center min-h-[60vh] text-center px-4 animate-slide-up">
                        <div className="p-8 rounded-full bg-green-500/10 mb-6 border border-green-500/30 shadow-[0_0_30px_rgba(34,197,94,0.2)]">
                            <i data-lucide="check-circle" className="w-24 h-24 text-green-400 animate-pulse"></i>
                        </div>
                        <h2 className="text-4xl font-bold text-white mb-2">Vote Recorded</h2>
                        <p className="mt-2 text-slate-400 max-w-md text-lg">Thank you for exercising your democratic right.</p>
                        
                        <div className="flex flex-col sm:flex-row gap-4 mt-12">
                            <button onClick={() => setPage('home')} className="px-8 py-3 bg-slate-800 hover:bg-slate-700 text-white rounded-xl transition-all border border-slate-600 hover:border-slate-500">Return Home</button>
                            <button onClick={resetVote} className="px-8 py-3 bg-gradient-to-r from-cyan-900/40 to-blue-900/40 hover:from-cyan-800/50 hover:to-blue-800/50 text-cyan-400 rounded-xl transition-all border border-cyan-800 hover:border-cyan-600 flex items-center justify-center gap-2 hover:shadow-lg hover:shadow-cyan-900/20">
                                <i data-lucide="refresh-cw" className="w-4 h-4"></i> Reset & Vote Again
                            </button>
                        </div>
                    </div>
                )
            }

            return (
                <div className="relative z-10 max-w-5xl mx-auto mt-6 px-4 pb-12">
                    <div className="flex items-center mb-10 gap-4 animate-slide-up">
                        <button onClick={() => voteStep === 'list' ? setPage('home') : setVoteStep('list')} className="p-3 rounded-full hover:bg-slate-800 text-white transition-colors border border-transparent hover:border-slate-700">
                            <i data-lucide="chevron-left" className="w-6 h-6"></i>
                        </button>
                        <div>
                            <h2 className="text-3xl font-bold text-white">
                                {voteStep === 'list' && "Select Candidate"}
                                {voteStep === 'face' && "Security Check"}
                                {voteStep === 'fingerprint' && "Final Verification"}
                                {voteStep === 'success' && "Vote Confirmed"}
                            </h2>
                            {voteStep === 'list' && <p className="text-slate-400 text-sm mt-1">Choose wisely. Your vote is final.</p>}
                        </div>
                    </div>

                    {voteStep === 'list' && (
                        <div className="grid grid-cols-1 md:grid-cols-3 gap-8">
                            {CANDIDATES.map((candidate, index) => (
                                <div 
                                    key={candidate.id} 
                                    className={`group relative bg-slate-900/60 backdrop-blur-md border border-slate-700 rounded-3xl overflow-hidden hover:border-cyan-500 hover:shadow-[0_0_30px_rgba(6,182,212,0.15)] hover:-translate-y-2 transition-all duration-500 animate-slide-up`}
                                    style={{ animationDelay: `${index * 100}ms` }}
                                >
                                    <div className={`h-28 bg-gradient-to-r ${candidate.color} relative overflow-hidden`}>
                                        <div className="absolute inset-0 bg-black/10 group-hover:bg-transparent transition-colors"></div>
                                        <div className="absolute -bottom-10 -right-10 w-40 h-40 bg-white/10 rounded-full blur-2xl group-hover:bg-white/20 transition-colors"></div>
                                    </div>
                                    <div className="px-8 pb-8 relative">
                                        <div className="absolute -top-14 left-8 w-28 h-28 rounded-2xl border-4 border-slate-900 bg-slate-800 flex items-center justify-center shadow-xl group-hover:scale-105 transition-transform duration-300">
                                            <i data-lucide="user" className="w-14 h-14 text-slate-400 group-hover:text-white transition-colors"></i>
                                        </div>
                                        <div className="absolute -top-10 right-8 text-5xl filter drop-shadow-lg animate-float" style={{ animationDelay: `${index * 500}ms` }}>{candidate.symbol}</div>
                                        
                                        <div className="mt-16 mb-8">
                                            <h3 className="text-2xl font-bold text-white group-hover:text-cyan-300 transition-colors">{candidate.name}</h3>
                                            <div className="flex items-center gap-2 mt-2 text-cyan-400 font-bold text-sm uppercase tracking-wider">
                                                {candidate.party} 
                                                <span className="w-1.5 h-1.5 rounded-full bg-cyan-500"></span>
                                                <span className="text-white">{candidate.symbol}</span>
                                            </div>
                                            <p className="mt-4 text-slate-400 text-sm leading-relaxed border-l-2 border-slate-700 pl-3 italic group-hover:border-cyan-500 transition-colors">"{candidate.agenda}"</p>
                                        </div>
                                        
                                        <button 
                                            onClick={() => startVoting(candidate)}
                                            className="w-full py-4 bg-slate-800 hover:bg-gradient-to-r hover:from-cyan-600 hover:to-blue-600 text-white font-bold rounded-xl transition-all duration-300 flex items-center justify-center gap-3 group/btn shadow-lg"
                                        >
                                            VOTE FOR {candidate.party.toUpperCase()}
                                            <i data-lucide="chevron-left" className="w-4 h-4 rotate-180 opacity-0 -ml-4 group-hover/btn:opacity-100 group-hover/btn:ml-0 transition-all"></i>
                                        </button>
                                    </div>
                                </div>
                            ))}
                        </div>
                    )}

                    {voteStep === 'face' && (
                        <div className="flex flex-col items-center justify-center py-8 animate-slide-up">
                            <div className="relative w-full max-w-lg aspect-video bg-black rounded-3xl overflow-hidden border border-slate-700 shadow-2xl ring-1 ring-white/10">
                                {cameraActive ? (
                                    <>
                                        <video ref={videoRef} autoPlay playsInline muted className="w-full h-full object-cover transform scale-x-[-1]" />
                                        {/* Scanning Grid Overlay */}
                                        <div className="absolute inset-0 z-20 opacity-30 bg-[linear-gradient(rgba(6,182,212,0.1)_1px,transparent_1px),linear-gradient(90deg,rgba(6,182,212,0.1)_1px,transparent_1px)] bg-[size:40px_40px]"></div>
                                        
                                        {/* Scanning Line */}
                                        <div className="absolute inset-0 border-4 border-cyan-500/50 rounded-3xl z-20 overflow-hidden">
                                            <div className="w-full h-1 bg-cyan-400 shadow-[0_0_15px_rgba(6,182,212,1)] absolute top-0 animate-[scan_2s_linear_infinite]" style={{ animation: 'scan 3s ease-in-out infinite' }}></div>
                                        </div>
                                        <style>{`@keyframes scan { 0%, 100% { top: 0%; opacity: 0; } 10% { opacity: 1; } 90% { opacity: 1; } 100% { top: 100%; opacity: 0; } }`}</style>

                                        <div className="absolute bottom-6 left-0 right-0 text-center z-30">
                                            <span className="bg-black/70 backdrop-blur text-cyan-400 px-6 py-2 rounded-full text-sm font-mono tracking-widest animate-pulse border border-cyan-500/30">SCANNING BIOMETRICS...</span>
                                        </div>
                                    </>
                                ) : (
                                    <div className="flex items-center justify-center h-full text-slate-500 flex-col bg-slate-900">
                                        <div className="w-16 h-16 rounded-full bg-slate-800 flex items-center justify-center mb-4 animate-bounce">
                                            <i data-lucide="camera" className="w-8 h-8 text-cyan-500"></i>
                                        </div>
                                        <span className="text-sm text-slate-400">Initializing Secure Camera Feed...</span>
                                    </div>
                                )}
                            </div>
                            <p className="mt-8 text-slate-300 text-lg">Please look directly at the camera for verification.</p>
                        </div>
                    )}

                    {voteStep === 'fingerprint' && (
                        <div className="flex flex-col items-center justify-center py-10 animate-slide-up">
                            <div className="p-12 bg-slate-900/80 backdrop-blur-xl border border-slate-700 rounded-[2.5rem] shadow-2xl text-center max-w-lg w-full relative overflow-hidden">
                                <div className="absolute inset-0 bg-gradient-to-b from-cyan-500/5 to-transparent pointer-events-none"></div>
                                <h3 className="text-2xl font-bold text-white mb-10 relative z-10">Final Verification</h3>
                                
                                <div 
                                    id="fingerprint-btn" 
                                    onClick={handleFingerprintScan} 
                                    className="w-40 h-40 mx-auto rounded-full border-2 border-dashed border-slate-600 flex items-center justify-center cursor-pointer hover:border-cyan-400 hover:bg-cyan-900/20 transition-all duration-300 group relative z-10"
                                >
                                    <div className="absolute inset-0 rounded-full bg-cyan-400/20 scale-0 group-hover:scale-100 transition-transform duration-500 rounded-full blur-xl"></div>
                                    <i data-lucide="fingerprint" className="w-20 h-20 text-slate-500 group-hover:text-cyan-400 transition-colors duration-300"></i>
                                </div>
                                
                                <div className="mt-10 relative z-10">
                                    <p className="text-slate-400 text-sm uppercase tracking-wide">Action Required</p>
                                    <p className="text-white font-medium text-lg mt-2">Place your finger on your laptop/device sensor to confirm vote for <br/><span className="text-cyan-400 font-bold text-2xl mt-2 block drop-shadow-glow">{selectedCandidate.name}</span></p>
                                </div>
                            </div>
                        </div>
                    )}

                    {voteStep === 'success' && (
                        <div className="flex flex-col items-center justify-center py-12 text-center animate-slide-up">
                            <div className="relative">
                                <div className="absolute inset-0 bg-green-500 blur-[60px] opacity-20 rounded-full"></div>
                                <div className="w-32 h-32 bg-gradient-to-br from-green-500 to-emerald-700 rounded-full flex items-center justify-center shadow-[0_0_50px_rgba(34,197,94,0.4)] mb-8 relative z-10 animate-bounce">
                                    <i data-lucide="check-circle" className="w-16 h-16 text-white"></i>
                                </div>
                            </div>
                            <h2 className="text-5xl font-bold text-white mb-4 tracking-tight">Vote Confirmed!</h2>
                            <p className="text-slate-400 max-w-md text-lg">Your vote for <span className="text-cyan-400 font-bold">{selectedCandidate.name}</span> has been securely recorded on the blockchain.</p>
                            
                            <div className="mt-6 p-4 bg-slate-900/50 rounded-xl border border-slate-800 font-mono text-xs text-slate-500">
                                Transaction ID: 0x8a7...3f29
                            </div>
                            
                            <div className="flex flex-col sm:flex-row gap-5 mt-12">
                                <button 
                                    onClick={() => { setVoteStep('list'); setPage('home'); }}
                                    className="px-10 py-4 bg-slate-800 hover:bg-slate-700 text-white font-bold rounded-xl transition-all border border-slate-600 hover:scale-105"
                                >
                                    Return to Dashboard
                                </button>
                                <button 
                                    onClick={resetVote}
                                    className="px-10 py-4 bg-gradient-to-r from-cyan-600 to-blue-600 hover:from-cyan-500 hover:to-blue-500 text-white font-bold rounded-xl transition-all shadow-lg shadow-cyan-900/40 hover:scale-105 hover:shadow-cyan-500/30 flex items-center gap-2"
                                >
                                    <i data-lucide="refresh-cw" className="w-5 h-5"></i> Vote Again
                                </button>
                            </div>
                        </div>
                    )}
                </div>
            );
          };

          const renderProfile = () => (
            <div className="relative z-10 flex flex-col items-center max-w-2xl mx-auto mt-8 animate-slide-up">
                <h2 className="mb-8 text-3xl font-bold text-white">Voter Profile</h2>
                <div className="w-full p-10 border border-slate-700/50 bg-slate-900/60 backdrop-blur-xl rounded-[2rem] shadow-2xl">
                    <div className="flex flex-col items-center md:flex-row md:items-start gap-10">
                        <div className="relative group">
                            <div className="absolute inset-0 bg-cyan-500 blur-xl opacity-20 group-hover:opacity-40 transition-opacity rounded-full"></div>
                            <div className="w-40 h-40 border-4 border-cyan-500 rounded-full bg-slate-800 flex items-center justify-center relative z-10 shadow-2xl animate-float">
                                <i data-lucide="user" className="w-20 h-20 text-slate-400 group-hover:text-white transition-colors"></i>
                            </div>
                            <div className="absolute bottom-2 right-2 p-3 bg-green-500 border-4 border-slate-900 rounded-full z-20"></div>
                        </div>
                        <div className="flex-1 space-y-6 text-center md:text-left pt-2">
                            <div>
                                <h3 className="text-3xl font-bold text-white">{currentUser.name}</h3>
                                <p className="text-cyan-400 font-medium text-lg mt-1">{currentUser.constituency}</p>
                            </div>
                            
                            <div className="grid grid-cols-1 gap-4 mt-6">
                                <div className="p-5 rounded-2xl bg-slate-800/50 border border-slate-700/50 hover:border-cyan-500/50 transition-colors">
                                    <label className="text-xs text-slate-500 uppercase font-bold tracking-wider">Aadhar Number</label>
                                    <p className="font-mono text-xl text-white mt-1">{currentUser.aadhar}</p>
                                </div>
                                <div className="p-5 rounded-2xl bg-slate-800/50 border border-slate-700/50 hover:border-cyan-500/50 transition-colors">
                                    <label className="text-xs text-slate-500 uppercase font-bold tracking-wider">Age</label>
                                    <p className="font-mono text-xl text-white mt-1">{currentUser.age} Years</p>
                                </div>
                            </div>
                        </div>
                    </div>
                    <div className="mt-10 pt-8 border-t border-slate-700/50 flex justify-between items-center">
                         <button onClick={() => setPage('home')} className="px-6 py-2 text-sm text-slate-400 hover:text-white transition-colors flex items-center gap-2">
                            <i data-lucide="chevron-left" className="w-4 h-4"></i> Back
                         </button>
                         <button onClick={handleLogout} className="flex items-center px-6 py-3 text-sm font-bold text-red-400 transition-colors border border-red-900/30 rounded-xl hover:bg-red-950 hover:border-red-500/50">
                            <i data-lucide="log-out" className="w-4 h-4 mr-2"></i> Logout
                         </button>
                    </div>
                </div>
            </div>
          );

          const renderAnnouncements = () => (
            <div className="relative z-10 max-w-3xl mx-auto mt-8 px-4 animate-slide-up">
                <div className="flex items-center mb-8 gap-4">
                    <button onClick={() => setPage('home')} className="p-3 rounded-full hover:bg-slate-800 text-white transition-colors">
                        <i data-lucide="chevron-left" className="w-6 h-6"></i>
                    </button>
                    <h2 className="text-3xl font-bold text-white">Campaign Announcements</h2>
                </div>
                
                <div className="space-y-6">
                    {ANNOUNCEMENTS.map((ann, i) => {
                        const candidate = CANDIDATES.find(c => c.id === ann.candidateId);
                        return (
                            <div key={ann.id} className="bg-slate-900/60 backdrop-blur border border-slate-700 p-8 rounded-3xl hover:border-cyan-500 transition-all hover:bg-slate-800/80 hover:-translate-y-1 animate-slide-up" style={{animationDelay: `${i*150}ms`}}>
                                <div className="flex items-center gap-5 mb-4">
                                     <div className="w-14 h-14 rounded-2xl border border-slate-600 bg-slate-800 flex items-center justify-center shrink-0">
                                        <i data-lucide="user" className="w-7 h-7 text-slate-400"></i>
                                    </div>
                                    <div>
                                        <h4 className="text-white font-bold text-lg">{candidate.name}</h4>
                                        <p className="text-xs text-cyan-400 font-bold uppercase tracking-wider mt-1">{candidate.party} • {ann.time}</p>
                                    </div>
                                </div>
                                <h3 className="text-2xl font-bold text-white mb-3">{ann.title}</h3>
                                <p className="text-slate-300 leading-relaxed text-lg">{ann.content}</p>
                            </div>
                        )
                    })}
                </div>
            </div>
          );

          const renderSettings = () => (
             <div className="relative z-10 max-w-2xl mx-auto mt-8 px-4 animate-slide-up">
                <div className="flex items-center mb-8 gap-4">
                    <button onClick={() => setPage('home')} className="p-3 rounded-full hover:bg-slate-800 text-white transition-colors">
                        <i data-lucide="chevron-left" className="w-6 h-6"></i>
                    </button>
                    <h2 className="text-3xl font-bold text-white">App Settings</h2>
                </div>

                <div className="bg-slate-900/60 backdrop-blur border border-slate-700 rounded-3xl overflow-hidden divide-y divide-slate-800/50">
                    <div className="p-6 flex items-center justify-between hover:bg-slate-800/50 cursor-pointer transition-colors">
                        <div className="flex items-center gap-4">
                            <div className="p-3 bg-cyan-900/20 rounded-xl text-cyan-400"><i data-lucide="fingerprint" className="w-6 h-6"></i></div>
                            <div>
                                <h4 className="text-white font-bold text-lg">Biometric Login</h4>
                                <p className="text-xs text-slate-400">Enable fingerprint for quick login</p>
                            </div>
                        </div>
                        <div className="w-12 h-7 bg-cyan-600 rounded-full relative shadow-inner"><div className="absolute right-1 top-1 w-5 h-5 bg-white rounded-full shadow-md"></div></div>
                    </div>
                    <div className="p-6 flex items-center justify-between hover:bg-slate-800/50 cursor-pointer transition-colors">
                        <div className="flex items-center gap-4">
                            <div className="p-3 bg-cyan-900/20 rounded-xl text-cyan-400"><i data-lucide="bell" className="w-6 h-6"></i></div>
                            <div>
                                <h4 className="text-white font-bold text-lg">Notifications</h4>
                                <p className="text-xs text-slate-400">Get updates on election dates</p>
                            </div>
                        </div>
                        <div className="w-12 h-7 bg-cyan-600 rounded-full relative shadow-inner"><div className="absolute right-1 top-1 w-5 h-5 bg-white rounded-full shadow-md"></div></div>
                    </div>
                </div>
             </div>
          );

          return (
            <div className="min-h-screen bg-slate-950 text-slate-200 font-sans selection:bg-cyan-500 selection:text-white">
              <StarryBackground />
              <main className="relative pb-20">
                {!isAuthenticated && renderLogin()}
                {isAuthenticated && page === 'home' && renderHome()}
                {isAuthenticated && page === 'profile' && renderProfile()}
                {isAuthenticated && page === 'vote' && renderVote()}
                {isAuthenticated && page === 'announcements' && renderAnnouncements()}
                {isAuthenticated && page === 'settings' && renderSettings()}
              </main>
            </div>
          );
        }

        const root = ReactDOM.createRoot(document.getElementById('root'));
        root.render(<App />);
    </script>
</body>
</html>